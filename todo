Define and code:
    - Python Dictionary
    - Pickling/Unpickling

// == complete

Convert:
    Javascript:
        JSON -> CSV
        JSON -> XML

        CSV -> JSON
        CSV -> XML

        XML -> JSON
        XML -> CSV

    Python:

        // JSON -> CSV 
        // JSON -> XML

        CSV -> JSON
        CSV -> XML

        XML -> JSON
        XML -> CSV